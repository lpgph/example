management:
  endpoint:
    health:
      show-details: always
  endpoints:
    web:
      exposure:
        include: '*'
server:
  port: 28081
spring:
  application:
    name: rabbitmq-produce-example
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848